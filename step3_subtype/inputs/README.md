# Step 3 Input Notes

This directory documents Step 3 private inputs. Runtime files are stored under `local_private_data/step3/inputs/` and generated files are stored under `local_private_data/step3/generated/`.

Place manually reviewed expression and report-level subclass files in `local_private_data/step3/inputs/manual_review/` before running scripts `06` and `07`.
