# Model Checkpoints

Place external model checkpoints under this directory or configure paths through environment variables. Checkpoints are not stored in this repository.

## Helper Scripts

- `T5_base_download.py`: downloads the base T5 checkpoint into the configured local checkpoint directory.
- `BART_large_cnn_download.py`: downloads the BART-large-CNN checkpoint into the configured local checkpoint directory.
